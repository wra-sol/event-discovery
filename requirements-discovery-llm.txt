# Optional: discover-source uses the same core deps as pyproject.toml (httpx); install with `uv sync`.
# It calls POST {LLM_API_BASE}/chat/completions (OpenAI-compatible).
#
# OpenCode Zen + Big Pickle (https://opencode.ai/docs/zen/):
#   export LLM_API_BASE=https://opencode.ai/zen/v1
#   export LLM_MODEL=big-pickle
#   export LLM_API_KEY=<your Zen API key>   # or OPENAI_API_KEY
# Sign in at Zen, add billing/credits as needed; model id is exactly "big-pickle".
# Note: during Big Pickle’s free period, Zen may use request data to improve the model
# (see Zen docs “Privacy”). Other Zen chat/completions models use the same base URL
# with their model ids (e.g. minimax-m2.5, glm-5, kimi-k2.5).
#
# Default if unset: LLM_API_BASE=https://api.openai.com/v1, LLM_MODEL=gpt-4o-mini,
# OPENAI_API_KEY or LLM_API_KEY.
#
# TypeScript / Vercel AI SDK: use @ai-sdk/openai-compatible with
# baseURL https://opencode.ai/zen/v1 and model big-pickle (same key as above).
# Example .env values (do not commit real keys):
LLM_API_BASE=https://opencode.ai/zen/v1
LLM_MODEL=big-pickle
LLM_API_KEY=<your Zen API key>